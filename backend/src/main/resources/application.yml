spring:
  application:
    name: hr-management-system
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/hr_management_db}
app:
  jwt:
    secret: ${JWT_SECRET}
    expiration: ${JWT_EXPIRATION:86400000}
